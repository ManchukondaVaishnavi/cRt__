#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int x,a,b;
    scanf("%d%d%d",&x,&a,&b);
    printf("%d",(x*a)-((x*b)+100));
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
