#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int a,b;
    scanf("%d%d",&a,&b);
    int i=a;
    while(i<=b){
        int c=0;
        for(int j=2;j<i;j++){
        if(i%j==0){
            c++;
        }
        }
        if(c==0){
        printf("%d ",i);
        }
        
        i++;
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
