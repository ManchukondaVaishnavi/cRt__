#include <stdio.h>
#include<stdlib.h>
int main() {
   int *a,*b,*c;
   a=(int*)malloc(sizeof(int));
   b=(int*)malloc(sizeof(int));
   scanf("%d%d",a,b);
   c=(int*)malloc(sizeof(int));
   *c=*a+*b;
   printf("%d",*c);
   return 0;
}
