#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int n;
    scanf("%d",&n);
    if(n>=1&&n<=10000000){
    //scanf("%d",&n);
    int a=1,b=1;
    int i=3,r=0;
    while(i<=n){
        r=a+b;
        a=b;
        b=r;
        i++;
    }
    printf("%d",b);
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
