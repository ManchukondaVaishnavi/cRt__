#include <stdio.h>
#include<string.h>

int main() {
  char s[100];
  scanf("%s",s);
int n=0;
  for(n=0;s[n]!='\0';n++);
for(int i=1;i<n;i=i+2){
   int j=0,h=s[i]-48;
   while(j<h){
       printf("%c",s[i-1]);
       j++;
   }
  // i++;
}
   return 0;
}
