#include <stdio.h>
#include<stdlib.h>
int main() {
   int *a,*b,*c;
   a=(int*)calloc(5,sizeof(int));
   b=(int*)malloc(5*sizeof(int));
   int i=0;
   for(i=0;i<5;i++){
   scanf("%d",a+i);
   }
   for(i=0;i<5;i++){
       scanf("%d",b+i);
   }
   for(i=0;i<5;i++){
      printf("%d\n",*(a+i));
   }
   return 0;
}
