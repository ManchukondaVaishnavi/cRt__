#include <stdio.h>

int sumofDigits(int n){
    if(n>0){
        int x=n%10;
        return x+sumofDigits(n/10);
    }
    else{
        return 0;
    }
}

int main(){
    printf("%d",sumofDigits(543));
    return 0;
    
}
